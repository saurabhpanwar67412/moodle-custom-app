import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PrizehistoryPage } from './prizehistory';

@NgModule({
  declarations: [
    PrizehistoryPage,
  ],
  imports: [
    IonicPageModule.forChild(PrizehistoryPage),
  ],
})
export class PrizehistoryPageModule {}
